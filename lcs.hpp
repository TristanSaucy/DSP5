#ifndef LCS_HPP
#define LCS_HPP
#include <string>
#include <fstream>
#include <iostream>
#include <sstream>
#include <vector>
class LCS
{
	public:
		LCS();
	private:
		std::ifstream input;

};
#endif
